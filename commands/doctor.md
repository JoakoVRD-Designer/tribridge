---
description: Check that claude, codex and agy are installed, signed in, and have the tribridge skill.
---

Run `tribridge doctor` and summarise it for the user: which agents are usable, and the exact fix
command for anything that is not (sign-in, missing CLI, `tribridge install <host>`).
